CREATE DATABASE waitlistapp;
CREATE TABLE user_signup(
    email varchar(20),
    password varchar(15),
    name varchar(25),
    uniquecode varchar(5),
    referralcode varchar(10),
    position int(10)
);
